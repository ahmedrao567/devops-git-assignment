Git is version control software that helps the programers to keep track of thier changes
and it is important because we may encounter such error and we may want to move back to our older version of our app
and git help us to track our changes and we can move revert our changes .

COMMANDS AND THIER PURPOSE
git clone      clone the repo from the github to the local compter machine
git status     Gives the status where you r whether at working direcory or at staging stage or at commit state also tells the deleted file modified files etc infomation in this
git add        stage the changes
git commit     save the changes physically on a file 
git branch     create a new branch
git checkout   use for changing the branches 
git merge      use to merge the branches together
git pull       pull the code from the github 
git push       push the updated code to the github

